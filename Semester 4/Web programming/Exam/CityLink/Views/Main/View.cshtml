@using CityLink.Models;

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title> See all cities </title>
    <style>
        .button-container {
            margin-top: 10px;
        }

        .button {
            display: inline-block;
            padding: 8px 16px;
            text-align: center;
            text-decoration: none;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            .button:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <div>
        @{
            List<City> cities = (List<City>)ViewData["cityList"];
        }

        <table class="">
            <tr>
                <th>id</th>
                <th>name</th>
                <th>county</th>
            </tr>
            @foreach (City c in cities)
            {
                <tr>
                    <td>@c.id</td>
                    <td>@c.name</td>
                    <td>@c.county</td>
                    <td>
                        <form action="/Main/LinkToCity" method="post" class="button">
                            <input type="hidden" name="id" value="@c.id" />
                            <input type="submit" value="Display links for that city" class="button" />
                        </form>
                    </td>
                </tr>
            }

        </table>

        <div class="button-container">
            <a href="/Main/..." class="button"> nothing yet </a>
            <!-- <a href="/Main/LinkToCity" class="button"> Link to a city </a> -->
        </div>

    </div>

</body>
</html>
